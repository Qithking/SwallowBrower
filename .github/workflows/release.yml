name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

env:
  PRODUCT_BUNDLE_IDENTIFIER: com.thking.SwallowBrower
  MACOSX_DEPLOYMENT_TARGET: "14.0"

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest

      - name: Show Xcode and SDK Info
        run: |
          xcodebuild -version
          xcodebuild -showsdks
          swift --version

      - name: Build Universal Binary
        run: |
          mkdir -p build
          
          # 构建 arm64 版本
          xcodebuild -project SwallowBrower.xcodeproj -scheme SwallowBrower -configuration Release -arch arm64 -derivedDataPath build/arm64 build CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
          
          # 查找 arm64 二进制文件
          ARM64_PATH=$(find build/arm64 -type f -name "SwallowBrower" -not -path "*.dSYM/*" 2>/dev/null | head -1)
          echo "=== arm64 binary: $ARM64_PATH ==="
          
          if [ -z "$ARM64_PATH" ] || [ ! -f "$ARM64_PATH" ]; then
            echo "Error: arm64 binary not found"
            ls -la build/arm64/Build/Products/Release*/ 2>/dev/null || true
            exit 1
          fi
          
          cp "$ARM64_PATH" build/SwallowBrower-arm64
          
          # 构建 x86_64 版本
          rm -rf build/x86_64
          xcodebuild -project SwallowBrower.xcodeproj -scheme SwallowBrower -configuration Release -arch x86_64 -derivedDataPath build/x86_64 build CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
          
          # 查找 x86_64 二进制文件
          X86_PATH=$(find build/x86_64 -type f -name "SwallowBrower" -not -path "*.dSYM/*" 2>/dev/null | head -1)
          echo "=== x86_64 binary: $X86_PATH ==="
          
          if [ -z "$X86_PATH" ] || [ ! -f "$X86_PATH" ]; then
            echo "Error: x86_64 binary not found"
            ls -la build/x86_64/Build/Products/Release*/ 2>/dev/null || true
            exit 1
          fi
          
          cp "$X86_PATH" build/SwallowBrower-x86_64
          
          # 合并为 Universal Binary
          lipo -create build/SwallowBrower-arm64 build/SwallowBrower-x86_64 -output build/SwallowBrower
          
          echo "=== Universal Binary Info ==="
          lipo -info build/SwallowBrower
          file build/SwallowBrower

      - name: Create App Bundle
        run: |
          # 创建 App Bundle 目录结构
          mkdir -p SwallowBrower.app/Contents/MacOS
          mkdir -p SwallowBrower.app/Contents/Resources
          
          # 复制二进制文件
          cp build/SwallowBrower SwallowBrower.app/Contents/MacOS/
          chmod +x SwallowBrower.app/Contents/MacOS/SwallowBrower
          
          # 生成 .icns 图标文件
          ICONSET_DIR="SwallowBrower.app/Contents/Resources/AppIcon.iconset"
          mkdir -p "$ICONSET_DIR"
          
          # 复制各种尺寸的图标到 iconset
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/16x16.png" "$ICONSET_DIR/icon_16x16.png" 2>/dev/null || true
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/16x16@2x.png" "$ICONSET_DIR/icon_16x16@2x.png" 2>/dev/null || true
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/32x32.png" "$ICONSET_DIR/icon_32x32.png" 2>/dev/null || true
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/32x32@2x.png" "$ICONSET_DIR/icon_32x32@2x.png" 2>/dev/null || true
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/128x128.png" "$ICONSET_DIR/icon_128x128.png" 2>/dev/null || true
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/128x128@2x.png" "$ICONSET_DIR/icon_128x128@2x.png" 2>/dev/null || true
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/256x256.png" "$ICONSET_DIR/icon_256x256.png" 2>/dev/null || true
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/256x256@2x.png" "$ICONSET_DIR/icon_256x256@2x.png" 2>/dev/null || true
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/512x512.png" "$ICONSET_DIR/icon_512x512.png" 2>/dev/null || true
          cp "SwallowBrower/Assets.xcassets/AppIcon.appiconset/512x512@2x.png" "$ICONSET_DIR/icon_512x512@2x.png" 2>/dev/null || true
          
          # 使用 iconutil 生成 .icns 文件
          iconutil -c icns "$ICONSET_DIR" -o "SwallowBrower.app/Contents/Resources/AppIcon.icns" 2>/dev/null || true
          rm -rf "$ICONSET_DIR"
          
          # 获取版本号
          VERSION=${GITHUB_REF#refs/tags/v}
          BUILD_NUM=$(git rev-list HEAD --count)
          
          # 创建 Info.plist
          cat > SwallowBrower.app/Contents/Info.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleDevelopmentRegion</key>
              <string>en</string>
              <key>CFBundleExecutable</key>
              <string>SwallowBrower</string>
              <key>CFBundleIconFile</key>
              <string>AppIcon</string>
              <key>CFBundleIdentifier</key>
              <string>com.thking.SwallowBrower</string>
              <key>CFBundleInfoDictionaryVersion</key>
              <string>6.0</string>
              <key>CFBundleName</key>
              <string>SwallowBrower</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>VERSION_PLACEHOLDER</string>
              <key>CFBundleVersion</key>
              <string>BUILD_PLACEHOLDER</string>
              <key>LSMinimumSystemVersion</key>
              <string>14.0</string>
              <key>LSUIElement</key>
              <false/>
              <key>NSHumanReadableCopyright</key>
              <string>Copyright © 2026. All rights reserved.</string>
              <key>NSPrincipalClass</key>
              <string>NSApplication</string>
          </dict>
          </plist>
          EOF
          
          sed -i '' "s/VERSION_PLACEHOLDER/${VERSION}/g" SwallowBrower.app/Contents/Info.plist
          sed -i '' "s/BUILD_PLACEHOLDER/${BUILD_NUM}/g" SwallowBrower.app/Contents/Info.plist
          
          # 复制 entitlements 文件
          if [ -f "SwallowBrower/SwallowBrower.entitlements" ]; then
            cp "SwallowBrower/SwallowBrower.entitlements" "SwallowBrower.app/Contents/Resources/SwallowBrower.entitlements"
          fi
          
          echo "=== App Bundle Structure ==="
          ls -la SwallowBrower.app/Contents/

      - name: Ad-hoc Sign App
        run: |
          # 使用 ad-hoc 签名（使用 - 表示 ad-hoc）
          codesign --force --deep --sign - SwallowBrower.app
          
          echo "=== Verify Signature ==="
          codesign -vvv SwallowBrower.app || true
          spctl --assess --type exec --verbose=2 SwallowBrower.app 2>&1 || true

      - name: Create DMG
        run: |
          brew install create-dmg
          
          mkdir -p dmg_temp
          cp -R SwallowBrower.app dmg_temp/
          
          # 使用 .icns 作为 DMG 卷图标
          ICON_PATH="dmg_temp/SwallowBrower.app/Contents/Resources/AppIcon.icns"
          
          echo "=== Using icon: $ICON_PATH ==="
          
          # 创建 DMG
          create-dmg \
            --volname "SwallowBrower" \
            --window-pos 200 120 \
            --window-size 500 350 \
            --icon "SwallowBrower.app" 150 175 \
            --app-drop-link 350 175 \
            --hide-extension "SwallowBrower.app" \
            --volicon "$ICON_PATH" \
            "SwallowBrower-${{ github.ref_name }}-universal.dmg" \
            "dmg_temp/"

          ls -la *.dmg

      - name: Generate Release Body
        id: release_body
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          
          if [ -n "$PREV_TAG" ]; then
            CHANGELOG=$(git log "$PREV_TAG"..HEAD --oneline --format='  - %s' 2>/dev/null | head -50 || echo "  - 无更新内容")
          else
            CHANGELOG=$(git log --oneline --format='  - %s' | head -20 2>/dev/null || echo "  - 无更新内容")
          fi
          
          {            
            echo "### 更新内容"
            echo "$CHANGELOG"
            echo ""           
            echo "### 安装说明"
            echo "  1. 下载并打开 DMG 文件"
            echo "  2. 将 SwallowBrower.app 拖入 Applications 文件夹"
            echo "  3. 如果提示"无法打开"，请在系统设置 > 隐私与安全性中允许运行"            
            echo ""
            echo "### 系统要求"
            echo "  - macOS 14.0 (Sonoma) 或更高版本"
          } > release_body.txt
          
          cat release_body.txt

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: release_body.txt
          files: SwallowBrower-*.dmg
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: SwallowBrower-build
          path: |
            SwallowBrower.app
            SwallowBrower-*.dmg
          retention-days: 7
